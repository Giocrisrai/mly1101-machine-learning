# Dependencias de la asignatura MLY1101.
# En Google Colab ya vienen todas instaladas: este archivo es para trabajo local.
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.13
jupyterlab>=4.0
pytest>=7.4
pyarrow>=14.0   # lectura de Parquet (notebook opcional de Waymo)
